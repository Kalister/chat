import express, { Request, Response } from 'express'

const router = express.Router();

// router.get('/', (req: Request, res: Response) => {
//     res.send('Express + TypeScript CHAT 1.0 Server');
// });

export default router;